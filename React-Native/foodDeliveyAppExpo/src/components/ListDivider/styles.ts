import {StyleSheet} from 'react-native';
import { theme } from '../../global/styles/theme';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 1,
        backgroundColor: theme.colors.ink06,
        marginTop: 1,
        marginVertical: 31,
        alignSelf: 'flex-end',
    },
})